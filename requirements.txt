anthropic>=0.40.0
pygame>=2.5.0
numpy>=1.24
Pillow>=10.0
python-dotenv>=1.0.0
# On Jetson prefer the apt build:  sudo apt install -y python3-opencv
# and then `pip install -r requirements.txt --no-deps opencv-python` is NOT needed.
# On a laptop, uncomment:
# opencv-python>=4.8
